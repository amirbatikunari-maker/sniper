# sniper WORK · 자동 개발 모드

비개발자용 사용법입니다.

## 1. 한 번만 준비
- `local-agent-start.bat`를 실행해 둡니다.
- Work에서 프로젝트 경로를 연결합니다.
- Supabase 로그인 후 프로젝트가 `로컬 Agent 연결됨`으로 표시되는지 확인합니다.

## 2. 매번 할 일
1. 작업 설명을 한두 문장으로 입력합니다.
2. `🚀 자동 개발 시작`을 누릅니다.
3. 기다립니다.

자동 모드는 다음을 순서대로 수행합니다.

`분석 → 설계 → 수정 → 실제 검증 → 실패 시 자동 재수정 → 성공 시 Git checkpoint`

## 3. 자동으로 하지 않는 것
- `.bat/.cmd/.ps1/.sh/.vbs/.psm1` 등 실행 가능한 스크립트 파일 적용
- 보호된 명령 실행
- 사용자 기존 변경과 섞어 적용

이 경우 화면에서 한 번 확인을 요청합니다.

## 4. 비용/품질
기본은 `BALANCED`입니다. 단계에 따라 모델과 추론 강도를 자동 조절합니다.

## 5. 문제가 생기면
자동 개발을 중단하고 WORK LOG 마지막 오류와 검증 결과를 확인합니다. 기존 변경은 SHA guard와 rollback으로 보호됩니다.
