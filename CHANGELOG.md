# v228 — 「과목 전체 빈 껍데기 정리」

실기는 회차마다 문항 수가 다르다. 14개짜리도 있고 20개짜리도 있다.
그런데 예전에 「문항수 채우기」로 18까지 자리를 만들어 둬서,
그 회차에 **원래 없는** 문항이 빈칸으로 남았다.

지우는 단추는 있었지만 **회차를 하나씩 열어야** 했다. 24회차면 24번이다.
과목 전체를 한 번에 훑는 단추를 붙였다.

## 지우기 전에 두 갈래로 가른다 — 이게 핵심

| | 무엇 | 어떻게 |
|---|---|---|
| **꼬리 껍데기** | 그 회차 마지막 자료보다 **뒤** 번호 | 원래 없는 문항 → **지운다** |
| **가운데 껍데기** | 자료와 자료 **사이**에 낀 빈칸 | 진짜 빠진 것일 수 있음 → **남긴다** |

이 자료로 재면 이렇게 갈린다.

```
꼬리 껍데기   44문항  → 지움
가운데 껍데기  1문항  → 남김 (2023년 3회 2번)
```

2018년 1회는 책에 15문항인데 16·17·18 이 비어 있다 → 꼬리다.
2023년 3회는 18문항인데 2번이 비어 있다 → 가운데다. OCR 이 「문제 02」 넉 자를
못 읽어 진짜로 빠진 것이라, 지우면 안 되고 원본에서 다시 넣어야 한다.

한 갈래로 뭉뚱그려 지웠으면 그 하나가 조용히 사라졌을 것이다.

## 화면

검수 툴바 → 「과목 전체 빈 껍데기 정리」
아래 텍스트칸에 회차별 번호까지 표로 뿌리고, **꼬리만** 지우는 단추가 나온다.
가운데 껍데기는 어느 회차 몇 번인지 적어 주니, 그 회차만 다시 넣으면 된다.

# v227 — 훑어보며 잡은 잠복 버그 둘

기능을 더 얹지 않고, 이미 넣은 것을 처음부터 다시 읽으며 찾은 것.

## ① refine 이 문항 차례를 뒤엉키게 할 수 있었다

v226 에서 마진을 «구간마다» 재게 고치면서, 문항을 구간별로 갈라 담았다가
다시 이어 붙인다. 그런데 이어 붙이는 순서가 **구간이 나온 순서**였다.

이 교재는 구간이 앞뒤로 나뉘어 있어 우연히 맞았지만,
구간이 책 안에서 섞여 나오는 PDF 라면 문항 차례가 통째로 뒤엉킨다.
붙인 뒤 **문서 순서로 다시 세운다**(`keep.sort(byDoc)`).

## ② reCut 이 «잘린 것 없음» 으로 거짓 보고하던 것

앞 작업에서 「중지」를 눌렀거나 오류로 끝나면 `STOP` 이 켜진 채 남는다.
그 상태로 「글자 잘린 문항만 다시 올리기」를 누르면 쪽을 재는 반복이
**첫 바퀴에 빠져나가** 아무것도 못 찾고 «없습니다» 로 끝났다.
시작할 때 내려 두고, 「중지」 단추도 이 작업에 걸리게 했다.

덧붙여 「옛 글자·옛 답안 지우기」가 켜져 있으면 검수에서 손으로 잘라 넣은 것도
덮인다는 것을 물어보기 전에 알려 준다.

## 확인만 하고 안 건드린 것

- `pathOf` 가 쓰는 `YEAR·SESS` — 자동 나누기는 «지금 연 회차» 안에서만 도니 맞다
- `앞서.max` 셈 — 이미 더한 값을 도로 빼고 세므로 두 번 더해지지 않는다
- `fillPoints` 가 `#slots` 를 빌려 쓰는 것 — 문항을 다시 누르면 `detail()` 이 덮어쓴다
- 워커의 base64 고침(v224)과 `slot` 받기(v213b) — 둘 다 살아 있다

# v226 — 남겨 뒀던 것 셋

## ① 배점 없는 문항 채우기

원본 스캔에서 오른쪽 끝 작은 «►점수 : 5점» 을 OCR 이 흘린 자리가 78개쯤 있다.
글자 층에 아예 없으니 코드로는 못 만든다 — 눈으로 보고 넣는 수밖에 없다.
그래서 **넣는 일을 빠르게** 만들었다.

검수 툴바 → 「배점 없는 문항 채우기」

- 빈 문항을 **그림 + 숫자칸** 한 줄씩 세로로 늘어놓는다
- 숫자 치고 **Enter** → 저장되고 다음 칸으로 내려간다
- 회차 총점이 100 이 되면 그 회차 줄이 **초록**으로 바뀐다

78개면 몇 분이면 끝난다.

## ② 「스스로 살펴보기」가 짧은 덩어리를 놓치던 것

오고초려(45쪽)·소방(18쪽)이 안 잡혔다. 원인이 둘이었다.

**열쇠가 쪼개졌다.** 오고초려 머리글 45개 중 16개는 OCR 이 번호를 통째로 못 읽어
`► 출제년도 : 04-2 / …` 만 남았다. 그래서 `#►`(23개)와 `►출제년도:#`(16개)로 갈라졌다.
→ «[번호] ► 출제년도» 꼴이면 번호가 없어도 같은 열쇠로 본다.

**번호를 엉뚱한 데서 읽었다.** 번호가 없는 줄에서 `출제년도 : 01-2` 의 **01** 을
번호로 집어 번호열이 엉켰다. 머리글은 «길게 이어 세는 것» 으로 가리는데
그게 망가지니 탈락했다. → 못 읽은 번호는 0 으로 두고, 0 은 끊긴 것으로 보지 않는다.

**창이 넓었다.** 24쪽 창으로는 45쪽짜리가 큰 덩어리에 묻힌다. 14쪽으로 좁혔다.

이제 다섯 덩어리를 **글자 하나 안 박고** 다 찾는다.

```
    2~858    «문제#»        1005개  마진 24pt
  859~899    «#►»             34개  마진 14pt
  900~1060   «핵심빈출#►»      141개  마진 41pt
 1061~1190   «#.»            536개  마진 49pt
 1191~1204   «#번»            10개  마진 17pt
```

## ③ 「옛 임시 자리 통째로 지우기」

v224 의 검수 화면은 «세 칸 다 빈 껍데기» 만 지울 수 있었다.
9001~9299 에는 그림이 든 것도 많아 절반이 남았다.
9001~9299(옛것)와 9301·9302·94NN·95NN(이번 판 이름표)을 갈라 표로 보여 주고,
**앞엣것만** 통째로 지우는 단추를 붙였다. 뒤엣것은 단추가 손댈 수 없다.

# v225 — v224 + 다른 갈래에서 빠져 있던 고침 여섯

v224 는 다른 갈래에서 만들어져, **v214~v222 의 고침이 안 들어가 있었다.**
그중 하나는 치명적이었다 — 그대로 두면 문항이 1720 → 1058 로 되돌아간다.

## 되살린 것

| 고침 | 무엇 | v224 |
|---|---|---|
| v216 | **구간별 마진** — 없으면 662문항이 앞 문제에 먹힌다 | 빠짐 |
| v216 | 회차 겹침을 덮어쓰지 않고 이어 붙이기 | 빠짐 |
| v214 | 구간별 성적표 + 초록 상자에 판번호 | 빠짐 |
| v217 | 「이 PDF 스스로 살펴보기」 | 빠짐 |
| v219 | 얇은 조각도 자르기 (「자를 영역이 없습니다」) | 빠짐 |
| v221 | 좌우 여백을 쪽마다 재기 (글자 잘림) | 빠짐 |
| v222 | 「글자 잘린 문항만 다시 올리기」 | 빠짐 |

## 지킨 것

v224 가 새로 넣은 것은 **하나도 안 건드렸다.**

- 워커가 그림을 «주소» 가 아니라 **base64** 로 보내는 고침 (403 의 진짜 원인)
- 검수 화면의 빈 껍데기 지우기 · 번호 다시 매기기
- `supabase-review.sql`

두 갈래를 줄 단위로 맞춰 보고, 양쪽 표시가 합본에서 다 살아 있는지 세어 확인했다.

## 합치면서 잡은 것 둘

- **`reCut` 이 첫 바퀴에 빠져나가던 것.** 앞 작업에서 `STOP` 이 켜진 채 남아 있으면
  쪽을 재는 반복이 곧바로 끊겨 «잘린 것 없음» 으로 끝났다. 시작할 때 내려 둔다.
  「중지」 단추도 이제 이 작업에 걸린다.
- **덮어쓰기 경고.** 「옛 글자·옛 답안 지우기」가 켜져 있으면 검수에서 손으로 잘라
  넣은 것도 덮인다. 물어보기 전에 알려 준다.

# v213d — 「전 회차 빈칸 모아보기」가 세로로 길게 늘어지던 것

`#cov` 는 62px 짜리 칸으로 나뉜 **격자**다.
한 회차만 볼 때는 그 안에 칸을 직접 뿌리니 잘 맞았는데,
전 회차 모아보기는 «회차 제목 + 그 회차 격자» 묶음을 통째로 집어넣었다.
그러면 그 묶음이 **62px 칸 하나**에 끼여서, 칸도 글자도 한 줄에 하나씩 꺾였다.
「9001년 / 제1회 — / 빈칸 71문 / 항」처럼 글자가 한 자씩 잘려 보인 게 그래서다.

모아보기일 때는 `#cov` 를 격자에서 풀어(`.covwrap`) 그냥 세로로 쌓는 상자로 쓴다.
회차 제목은 스크롤에 붙어 다니게 해서, 길게 내려도 지금 어느 회차인지 안 놓친다.

# v213c — 한 권을 통째로

「200쪽씩 네 번, 그다음 머리글 모양 바꿔서 또 세 번」은 사람이 할 일이 아니었다.
PDF 하나 넣고 단추 한 번 누르면 끝나게 고쳤다.

## 쪽마다 «어느 덩어리인가» 를 보고 규칙을 갈아 끼운다

다산 기출문제집 한 권(1204쪽)에는 성격이 다른 다섯 덩어리가 들어 있다.
머리글 생김새가 전부 달라서 하나로 맞춰 두면 나머지는 반드시 0개가 나왔다.

| 덩어리 | 쪽 | 머리글 | 문항 |
|---|---|---|---|
| 기출문제 | 1~853 | `문제 01` | 1007 |
| 오고초려 | 854~898 | `01 ► 출제년도` | 47 |
| 핵심 빈출 146제 | 899~1050 | `핵심 빈출 3 ► 출제년도` | **146** |
| 연도별 단답 | 1051~1186 | `1.` `2.` | 470 |
| 소방설비기사 | 1187~1204 | `09 번.` | 13 |

**1683문항.** 핵심빈출이 책 표지의 「146제」와 정확히 맞는다.

## 회차가 없는 덩어리에 이름표를 지어 준다

| 덩어리 | 연도 | 회차 |
|---|---|---|
| 기출 | 실제 (2008~2026) | 실제 |
| 오고초려 | 9301 | 20문항씩 1회·2회… |
| 핵심빈출 | 9302 | 20문항씩 1회~8회 |
| 단답 | 94NN | 9419년 3회 = 2019년 3회 단답 |
| 소방설비 | 95NN | 실제 회차 |

기출과 절대 안 겹치고, 숫자만 보고 무엇인지 되짚을 수 있다.
예전에 쓰던 9001·9002·9003 과 **일부러 다른 번호**를 골랐다 — 섞이면 못 가른다.

오고초려·핵심빈출은 장 표시가 `[1] [2] [3] [1] [2]…` 로 단원마다 1로 돌아간다.
그걸 회차로 삼으면 번호가 뒤엉켜서, 나온 순서대로 20문항씩 끊었다.

## 같이 잡은 버그 둘

- `HDR_BOOK` 이 화살표 `‣` `▶` 만 받고 있었다. **이 책이 쓰는 건 `►` 다.**
  그래서 오고초려·핵심빈출이 통째로 0개였다.
- 되풀이 머리글 거르개가 `핵심 빈출 2 ► 출제년도 : …` 까지 잡아먹었다.
  출제년도가 붙은 줄은 진짜 머리글이므로 살려 둔다.
- 소방설비기사는 `09 번.` 처럼 「번」이 끼어 있어 번호 규칙에 안 걸렸다. 갈래를 하나 더 뒀다.

## 배점 되찾기·회차 상태표는 기출에만

문제집·단답은 배점 자체가 없다. 총점 100 규칙을 거기 들이대면 헛것이 나온다.

## 쪽 범위는 이제 선택

비워 두면 통째로 돈다. 중간에 끊겼거나 한 덩어리만 다시 넣을 때만 적으면 된다.

# v213b — 검수 화면

## 답 슬롯에도 자르기

문제 슬롯에만 있던 「이 그림 자르기」를 **답 슬롯**에도 달았다.
답 그림 위쪽에 문제가 딸려 들어간 것이 있었는데 손댈 방법이 없었다.
편집기가 어느 슬롯을 자르고 있는지 기억해서, 그 자리를 덮어쓴다
(다른 자리로 넣으면 예전처럼 후보로 쌓인다).

## 문제·답 자동으로 나눠 넣기

「AI로 경계 찾기 → 선 확인 → 칸 배정 → 저장」 네 걸음을 단추 하나로 줄였다.
답 슬롯이 빈 문항의 문제 슬롯에만 나온다.

회차 통째로 도는 「답 빈 문항 자동으로 나누기」도 툴바에 넣었다.
**경계를 못 찾으면 건드리지 않는다** — 억지로 자른 그림을 되돌리는 것보다
안 자르고 넘어가는 쪽이 싸다. 건너뛴 번호는 기록칸에 남는다.
「중지」로 멈출 수 있고 그때까지 한 것은 남는다.

## 회차 미상 정리

연도 9000 이상(9001 · 9002 …)으로 들어간 자료를 묶음별로 세어
표로 뿌리고, **무엇을 해야 하는지**까지 적는다.

- 세 칸 다 빈 껍데기 → 그 자리에서 지우는 단추
- 자료가 든 것 → 원본 PDF 로 다시 넣는 편이 낫다 (v213 은 회차를 읽는다)
- 원래 회차가 없는 자료(단답 정리본) → 그대로 두는 게 맞다

자료가 한 줄이라도 든 문항은 이 단추로 절대 안 지운다.

## 워커

`/split-hint` 가 `slot` 을 받는다. 답 그림이면 «위쪽에 문제가 섞여 들어왔는지»를
묻고, 처음부터 답만 있으면 found 를 false 로 돌려준다.

# v213 — 스캔 기출문제집이 «문제 0개» 로 끝나던 것

다산에듀 『전기기사 실기 기출문제집』(OCR 스캔본, 1204쪽)을 넣으면
1005개 머리글을 잡아 놓고도 마지막에 전부 버려 0개가 나왔다. 원인이 셋이었다.

## 1. 회차 표시에 «년» 을 요구했다  — 이게 0개의 직접 원인

```js
const ROUND = /(\d{4})\s*년\s*제?\s*(\d)\s*회/;   // 옛것
```

이 책 머리글에는 「년」이 없다.

```
2025 제3회전기기사실기          ■ 합격률 63.0%
2026     전 기 기 사 실 기      1회
```

회차를 한 번도 못 찾으니 `items.filter(it => it.year && it.session)` 에서 1005개가
통째로 걸러졌다. 생김새 네 가지로 나눠 받고(`ROUND_SET`), 본문 숫자에 끌려가지
않도록 **쪽 맨 위 네 줄에서만** 찾는다.

## 2. 쪽 위쪽 60pt 를 통째로 버렸다  — 머리글 45% 가 사라졌다

되풀이 머리글을 피하려던 `if(top < 60) continue;` 인데,
이 책은 앞 문제가 앞쪽에서 끝나면 **다음 쪽 맨 위**에서 새 문제가 시작한다.
세어 보니 「문제 NN」 머리글 1005개 중 **449개(45%)** 가 60pt 위에 있었다.
높이로 자르지 않고 생김새(`RUNNING_HEAD`)로 거른다.

## 3. 머리글 글자가 날아간 쪽

「문제 02」 넉 자만 안 읽히고 옆의 「출제년도 : 09. 12. 23. ►점수 : 3점」은
멀쩡한 쪽이 있다. 그러면 그 문제가 앞 문제에 먹혀 두 문제가 한 장이 됐다.
출제년도와 점수가 한 줄에 같이 있으면 머리글로 보고, 번호는 앞 번호 + 1 을 준다.
pdf.js 가 글자 조각을 쓰인 순서로 주기 때문에 「►점수 … 문제 01 …」로 앞뒤가
바뀌어 들어오는 쪽도 있어서, 줄 어디에 있든 「문제 NN」을 찾도록 했다.

결과 — **문항 1007개 · 회차 57개.** 조각 순서를 거꾸로 뒤집어도 같은 수가 나온다.

## 같이 넣은 것

- **배점 되찾기.** 오른쪽 끝 작은 「►점수 : 5점」을 OCR 이 잘 흘린다(1007개 중 101개).
  실기는 회차 총점이 반드시 100점이라, 빈칸이 하나만 남으면 나머지를 더해서 뺀 값이
  곧 정답이다 — 추측이 아니라 계산이다. 남은 것은 같은 문제가 실린 다른 회차에서
  가져오되, 값이 갈리면 안 쓴다.
- **회차별 상태표.** 훑어보기가 끝나면 회차마다 문항 수·배점 합·판정·손볼 것을 찍는다.
  배점 합 100 은 «자료가 온전하다», 미달은 «그만큼이 안 읽혔다» 는 뜻이다.
- **몇 쪽부터 · 몇 쪽까지.** 1000쪽짜리를 통째로 돌리면 브라우저가 그림을 만들다 멎는다.
  200쪽씩 끊어 넣으면 같은 결과가 나오고, 끊겨도 그 범위만 다시 돌리면 된다.
- **자동 변환 화면.** 「필기 / 실기」 중 어느 쪽을 눌러야 하는지 먼저 정해 주고,
  서술형인 실기를 기본 탭으로 뒀다.
- 배점 「5정」「5§」, 「답 안 작 성」처럼 사이가 벌어진 OCR 오독도 받아 준다.

