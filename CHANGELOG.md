# sniper — 변경 기록

## v18 (현재)

**새로 만들기 — 첨부가 사진에서 «아무 파일» 로**
- 파일·폴더·zip 을 그대로 받습니다. 대화창에 끌어다 놓거나, 눌러서 고르거나,
  Ctrl+V 로 붙여넣으면 됩니다. 폴더는 통째로 끌어다 놓아도 안을 재귀로 훑습니다.
- zip 은 **브라우저에서 풀어서** 안의 코드 파일을 읽습니다(JSZip). Worker 는 zip 을
  모르고, 알 필요도 없습니다 — 텍스트/코드 파일은 프롬프트 안에 본문으로 들어가고
  사진만 예전처럼 base64 로 갑니다. 그래서 **이 기능들은 Worker 를 안 고쳐도 동작합니다.**
- `node_modules`, `.git`, `dist`, `*.min.js`, lock 파일은 넣어봐야 토큰만 먹으므로 뺍니다.
- 첨부한 파일은 가운데 목록에 뜨고, 체크를 꺼서 «이건 빼고 보내기» 를 할 수 있습니다.
  옆에 대략 몇 토큰인지 같이 보여 줍니다.
- 사진은 4장 → 8장. bmp·avif 처럼 모델이 못 읽는 형식과 큰 스크린샷은 캔버스로
  1600px·png/jpeg 로 바꿔서 보냅니다. 형식이 안 맞아 요청 전체가 400 으로 죽던 길을 막았습니다.

**결과를 여러 파일로 — GitHub 에 그대로 올릴 수 있게**
- 「여러 파일 (GitHub용)」로 두면 `index.html` + `assets/style.css` + `assets/app.js` +
  `README.md` 처럼 나눠서 받습니다. ZIP 으로 내려받아 압축만 풀면 저장소에 그대로
  올라갑니다(`.nojekyll` 포함). 「한 파일 HTML」도 그대로 남아 있습니다.
- 형식은 모델에게 `<<<FILE: 경로>>> … <<<ENDFILE>>>` 표시를 쓰게 시켜서 프론트가 가릅니다.
  모델이 표시를 잊고 ```` ```html index.html ```` 로 뱉거나 그냥 HTML 한 덩어리로 뱉어도
  받아내는 예비 파서를 2단 더 뒀습니다.
- 이어서 수정할 때는 프로젝트 전체를 다시 실어 보냅니다.

**Claude 추가**
- provider 에 Claude 가 생겼습니다. 이건 **Worker 배포가 필요한 유일한 항목** 입니다 —
  `worker-ai-build-claude.js` 를 sniper-ai 에 붙여넣고 `ANTHROPIC_API_KEY` 를 넣으세요.
  안 붙였을 때는 프론트가 「Worker 에 Claude 경로가 아직 없습니다」로 알려 줍니다.
- 모델 이름을 직접 적는 칸도 뒀습니다. 나중에 모델이 바뀌어도 화면을 안 고쳐도 됩니다.

**파일 목록과 미니맵**
- 가운데 칸에 「첨부한 파일 / 만들어진 파일」 두 갈래 트리. 아무 파일이나 누르면
  오른쪽에 코드가 뜨고, 그 옆에 미니맵이 붙습니다. 끌어서 위치를 옮길 수 있습니다.
  올린 zip 안의 파일도 똑같이 눌러서 볼 수 있습니다.

**미리보기에서 고친 것 3가지**
- 여백. 미리보기 폭을 휴대폰/태블릿/꽉 채우기로 바꿔 볼 수 있습니다. 「여백 줄이기」·
  「밝은 배경」·「가독성 우선」 같은 요청은 매번 타이핑하는 대신 칩으로 눌러 둡니다.
- 여러 파일이 된 뒤에도 미리보기가 되게, css·js 를 html 안으로 «합쳐서» 보여 줍니다.
  격리한 iframe 은 부모가 만든 blob: 주소를 못 읽기 때문에(출처가 달라짐) 링크를
  따라가게 둘 수가 없었습니다. 내려받는 파일은 나뉜 채 그대로입니다.
- 격리된 iframe 안에서 `localStorage` 를 만지면 예외가 나서, 「저장」 기능이 든 페이지는
  미리보기가 통째로 멈췄습니다. 미리보기에서만 쓰는 임시 저장소를 끼워 넣어 막았습니다.

**숨은 위험 하나**
- 「새 탭에서 열기」가 blob: 주소를 그냥 열고 있었습니다. blob: 은 이 블로그와 «같은 출처»
  라서, 만들어진 코드가 마음만 먹으면 내 로그인 토큰(localStorage)을 읽을 수 있었습니다.
  껍데기 한 장을 띄우고 그 안의 sandbox iframe 에 내용을 넣어 그 길을 끊었습니다.

**휴대폰**
- 좁아지면 「대화 / 파일 / 결과」 탭으로 갈립니다. 예전에는 `@media(max-width:900px)` 에서
  세로로 쌓기만 해서 미리보기가 화면 밖으로 밀려나 있었습니다.

## v17

**새로 만들기(바이브 코딩) 추가**
- 설명 + 참고 사진(최대 4장) + 참고 사이트 주소를 넣으면 완전히 새로운 미니 웹앱 하나를
  즉석에서 만들어 오른쪽에 바로 미리보기(iframe)로 보여주는 `build.html` 화면을 추가했습니다.
  대화하듯 "이 부분 바꿔줘"를 계속 보내면 지금까지 만든 코드를 기반으로 다시 완성해서 돌려줍니다.
  완성되면 코드 보기 / 새 탭 열기 / .html 파일로 다운로드할 수 있습니다.
- 참고 사진은 기존 AI 대화가 쓰던 방식 그대로(base64 첨부, GPT/Gemini 둘 다 인식) 재사용했습니다.
- 참고 사이트는 브라우저가 아니라 **Worker가 대신 읽어와서** 텍스트만 추려 참고합니다
  (브라우저에서 남의 사이트를 직접 fetch하면 CORS로 대부분 막힘). 사설 IP·localhost 주소는
  차단합니다. 사이트를 그대로 스크린샷 떠서 베끼는 기능은 이번 버전엔 없습니다.
- `sniper-ai` Worker(`src/index.js`)에 `/ai/build` 엔드포인트를 추가했습니다 — 기존 `/ai/chat`,
  `/agent/run` 은 손대지 않았고 새 엔드포인트만 더했습니다. **이 Worker는 별도 배포가 필요합니다**
  (Cloudflare 대시보드 sniper-ai → 코드 편집 → 새 코드 붙여넣기 → 배포).

**숨김(hidden) 버그 3건 추가로 발견해 고침**
- Work 화면의 로그인 카드가 로그인에 성공해도 안 사라지고 화면 위에 빈 칸으로 계속 떠 있던 문제
  (`.login-page`가 `display:grid`를 강제해서 `hidden` 속성이 무시되고 있었음).
- 새로 만들기 화면에서도 같은 이유로 미리보기 안내 문구와 코드 패널이 잘못 보이던 문제.
- 검색창의 "최근 검색어" 목록도 같은 이유로 계속 떠 있던 문제(`index.html`/`post.html`/`about.html`
  공용 `style.css`). 같은 파일의 `.cmdPal`은 이미 이 문제가 고쳐져 있었던 걸로 봐서, 예전에도 한 번
  발견됐다가 다른 곳엔 안 옮겨진 것으로 보입니다 — `[hidden]{display:none}` 을 명시하는 규칙을
  기억해 두면 좋을 버그 패턴입니다.

## v16

**어디서 복사해도 받는 붙여넣기**
- 글쓰기 화면 에디터가 Editor.js 기본 붙여넣기에만 기대고 있어서, GPT·Gemini·
  Claude 답변처럼 div 로 겹겹이 쌓인 내용을 복사해오면 못 받거나 다 뭉개졌습니다.
  더 너그러운 파서(`pasteHtmlToBlocks`/`pasteTextToBlocks`)를 새로 붙여, 제목·
  목록·표·인용·코드는 그대로 살리고 나머지는 문단으로라도 반드시 남기게 했습니다.
  html 이 없을 때(순수 텍스트 복사)는 마크다운(#, -, ```, >)까지 알아봅니다.
- AI 창의 «본문에 넣기» 도 기존 파서가 못 건질 때는 같은 파서를 예비로 씁니다.

**비밀글**
- 글쓰기 화면에 🔒 토글 추가. 켜면 로그인한 나만 볼 수 있습니다.
  목록·글 화면에서 비관리자에게는 아예 안 보이고, 배지로만 표시됩니다.
  `blog_posts.secret` 컬럼이 필요합니다 → `04-secret-private.sql` 실행.

**비공개 게시판**
- 게시판 관리에 «비공개» 토글 추가. 켜면 하위 게시판까지 포함해 안의 글이
  전부 비밀글로 바뀝니다(끄는 건 게시판 표시만 되돌리고 개별 글은 그대로 둠).
  `blog_categories.is_private` 컬럼이 필요합니다 → 같은 SQL.

**글쓰기 화면에 사이드바 고정**
- 글쓰는 중에는 카테고리·검색 등 왼쪽 게시판이 아예 안 보이던 문제를 고쳤습니다.
  다른 화면과 같은 사이드바를 붙였습니다(모바일에서는 ☰ 로 여닫기, 동일).

**Work Agent 버그 두 개**
- `local-agent-start.bat/.ps1` 이 허용 주소를 옛 값으로 덮어써서, 실제 배포
  주소(`sniper-web.pages.dev`)에서는 로컬 Agent 접속이 전부 막혀 있었습니다.
- 브라우저 폴더 선택이 최상위 파일만 읽고 `src/` 같은 하위 폴더를 못 읽던
  버그 — 재귀적으로 읽도록 고쳤습니다.

## v15

**화면 정리**
- 왼쪽 위 중복 제거. 제목 아래 한 줄(`tagline`)을 프로필 카드에서도 쓰고 있어
  같은 문구가 두 번 나왔습니다. 프로필용 `owner_role` 을 따로 만들어 갈랐습니다.
  이제 «설정»과 «프로필 고치기»에서 각각 수정합니다.
- 설정에 「프로필 카드 보이기/숨기기」, 「제목 아래 한 줄 보이기/숨기기」 추가.
- 맨 아래 «직접 만들어 쓰는 기록장» 하드코딩 문구 삭제 → 설정에서 입력.
  비워 두면 아무것도 안 나옵니다.
- 왼쪽 사이드바 폭을 오른쪽 AI 패널 폭(`--aiw`, 376px)에 묶었습니다.
  한 곳만 고치면 좌우가 같이 움직입니다. 모바일 서랍은 270px 그대로.

**허용 Origin 확정**
- 실제 배포 주소가 `https://sniper-web.pages.dev` 로 확인되어 기본값에 반영했습니다.

**Work Agent 에 Gemini 추가**
- `/agent/run` 이 GPT 와 Gemini 를 모두 씁니다. 모델 ID 가 `gemini-` 로 시작하면
  Gemini 경로로 갑니다 (`generateContent` + `responseSchema`).
- OpenAI JSON Schema 의 `additionalProperties`/`strict` 는 Gemini 가 모르므로 걷어내고 보냅니다.
  스키마를 거부하면 스키마 없이 한 번 더 시도합니다.
- Gemini 에는 이어붙일 응답 ID 가 없어 각 단계가 무상태로 돕니다 (`id: null`).
  Work 화면은 이미 `if(j.id)` 로 막고 있어 그대로 동작합니다.
- 추론 단계는 `thinkingBudget` 으로 옮깁니다 (low 2048 … max 32768).
- `/ai/models` 의 `agentModels` 에 Gemini 모델이 함께 나옵니다
  (Worker 에 `GEMINI_API_KEY` 가 있을 때만).
- **자동 조절이 공급자를 갈아치우지 않습니다.** Gemini 를 골라두면 자동 조절도
  Gemini 안에서만 모델을 바꿉니다. 예전에는 GPT 로 되돌아갔습니다.

**위험 실행 스크립트: 쓰기와 실행 분리**
- `.bat .cmd .ps1 .sh .vbs .psm1` 은 이제 «막기»가 아니라 «2차 승인» 방식입니다.
- AI 가 이런 파일을 만들거나 고치려 하면 서버가 409 `SCRIPT_APPROVAL_REQUIRED` 로 막고
  승인번호를 돌려줍니다. Work 화면은 내용을 기록창에 찍고 확인을 받은 뒤에만 다시 보냅니다.
- 승인번호는 **그 경로들에만** 유효하고, **1회용**이며, 2분 뒤 만료됩니다.
- 자동 적용을 켜 놨어도 이 확인은 건너뛰지 않습니다.

**Origin 문제 진단 개선**
- 거부된 Origin 을 실제 값과 함께 화면·응답에 남깁니다
  (`ORIGIN_NOT_ALLOWED` + 허용 목록). 어느 주소를 넣어야 하는지 바로 보입니다.
- ⚠ 기본 허용 목록은 «추측»입니다. 배포한 블로그 주소가 다르면 반드시 지정하세요:
  `set SNIPER_WORK_ORIGINS=https://실제-블로그주소,http://127.0.0.1:5500`

**공용 파일 일원화**
- `shared/ai-chat.js` 가 유일한 원본입니다. `tools/sync-shared.sh` 로 블로그·뷰어에 복사합니다.
- 양쪽 파일 맨 위에 «직접 고치지 말 것» 안내를 넣었습니다.

**등급(tier) 값 수정 — 실제로 안 먹던 버그**
- `ai-chat.js` 는 등급을 «균형/최고급/빠름» 한글로 비교하는데 Worker 가
  `medium/high/low` 를 보내고 있었습니다. 라벨 우연 일치로 균형·빠름만 먹고
  **최고급을 눌러도 Sol 로 안 바뀌었습니다.** 실제 브라우저로 확인해 잡았습니다.

## v14 (현재)

**고친 것 — 이게 없으면 실제로 안 돌아갔던 것들**

1. **AI 대화가 404 였습니다.**
   `ai-chat.js`(블로그·뷰어 공용)는 `/ai/chat` 을 부르는데, 워커에는 `/ai/health`,
   `/ai/models`, `/agent/run` 밖에 없었습니다. `/ai/chat` 을 새로 넣었습니다.
   - OpenAI Responses API 스트리밍 + Gemini `streamGenerateContent` 둘 다 지원
   - 이미지·PDF 첨부 전달
   - 응답은 `ai-chat.js` 가 파싱하는 SSE 형식 (`data: {"delta":"…"}` → `data: [DONE]`)
2. **Gemini 목록이 비어 있었습니다.** `/ai/models` 가 `gemini: []` 를 반환해
   "GPT/Gemini 바꿔 쓰기" 가 사실상 GPT 전용이었습니다.
   이제 Gemini API 에 모델 목록을 직접 물어봐 채웁니다(30분 캐시, 실패 시 고정 목록).
   새 Gemini 모델이 나와도 코드를 고칠 필요가 없습니다.
3. **Work Agent 세션 쿠키가 안 붙었습니다.**
   `SameSite=Lax` 라 배포된 https 페이지 → `http://127.0.0.1:8788` 요청에서 쿠키가 빠졌고,
   사실상 `http://127.0.0.1:5500` 에서 열 때만 동작했습니다.
   - 쿠키를 `Secure; SameSite=None` 으로 변경
   - `/session` 이 토큰을 함께 반환하고, Work 화면이 **메모리에만** 들고
     `x-agent-token` 헤더로 보냅니다 (localStorage 저장 안 함)
4. **사설망 접근 프리플라이트.** https 페이지에서 로컬 주소를 부를 때 크롬이 요구하는
   `Access-Control-Allow-Private-Network` 응답 헤더를 추가했습니다.
5. **허용 Origin 정리.** Local Agent 기본값에 sniper 주소가 빠져 있었습니다(뷰어 주소만 있었음).
   워커 `ALLOWED_ORIGINS` 도 같이 맞췄습니다.

**막은 것**

6. **AI 가 실행 스크립트를 못 쓰게.** `.bat/.ps1/.cmd/.sh` 는 읽기·색인까지만 허용하고
   쓰기 대상에서 제외했습니다. 실행 명령에 `npm/npx/node` 가 있어서,
   AI 가 스크립트를 만들고 그걸 실행시키는 경로가 열려 있었습니다.

**정리**

7. `Work` 화면이 `/ai/models` 의 `agentModels`(추론단계 지원 모델)를 쓰도록 분리.
   대화용 모델과 에이전트용 모델이 섞이지 않습니다.
8. 서비스워커 캐시 이름 `sniper-shell-v13` → `v14`. 이전엔 v12 로 멈춰 있어
   새 배포가 반영되지 않을 수 있었습니다.
9. `CHANGELOG_V11/12/13.md` 를 이 파일 하나로 합치고,
   중복 파일 `wrangler-ai.toml.txt` 를 삭제했습니다
   (실제 워커 설정은 `sniper-ai-worker/wrangler.toml`).

## v7–v13 요약

- v7~v9: Local Agent 인증(Supabase + HttpOnly 세션), 프로젝트 루트 바인딩, 검증 프로필,
  심볼릭 링크 탈출 차단, atomic write, 작업 동시 실행 잠금.
- v8: 적용 전 파일 SHA-256 precondition 검사, 비밀값 파일·내용 차단, 파일별 선택 적용.
- v12: realpath 바인딩, 프로젝트 규칙 저장, 비용/품질 전략, AI 컨텍스트 비밀값 마스킹.
- v13: 프로젝트 구조 인덱스(`/project-index`), 세션 복원 범위 확대, stage 별 JSON Schema 강화.
