# ═══════════════════════════════════════════════════════════════
#  sniper — 블로그 «화면» 을 올리는 설정
#  ───────────────────────────────────────────────────────────────
#  이 저장소에는 HTML·CSS·JS 파일만 있습니다. 서버 코드가 없으므로
#  main 을 쓰지 않고, 폴더째로 정적 파일만 올립니다.
#
#  ⚠ AI 중계 Worker(sniper-ai)는 «다른» Worker 입니다.
#    그 설정은 이 파일이 아니라 sniper-ai 쪽에서 따로 관리합니다.
#    (예전에 여기 들어 있던 AI 설정은 wrangler-ai.toml.txt 로 옮겨 뒀습니다)
# ═══════════════════════════════════════════════════════════════

name = "sniper"
compatibility_date = "2026-08-01"

[assets]
directory = "./"
